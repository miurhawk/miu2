import * as React from "react";
import {DetailCol, FullPhoto} from "../styles";



const Playlists = () => {

  return (
    <>
       <p>Each gym has a leaderboard, with points distributed relative to your highest send.
           Win monthly <span style={{fontWeight: `bold`}}>gear giveaways</span> and solidify your position as the next <span style={{fontWeight: `bold`}}>Janja</span> (of BoulderWorld West).
       </p>

    </>
  );
};
// <FlexRowFill img={shoesImg} style={{minHeight: `120vh`}} />

export default Playlists;
