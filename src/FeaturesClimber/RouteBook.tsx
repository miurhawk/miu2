import * as React from "react";
import {DetailRow, BookButton, FullPhoto, SmallDetailCol, DetailCol} from "../styles";


const RouteBook = () => {
  return (
    <>
      <p>Stuck on your project? Choose the red pill and watch the <span style={{fontWeight: `bold`}}>beta clips</span> uploaded by your routesetter.</p>
    </>
  );
};

export default RouteBook;
