import * as React from "react";
import { DetailRow, FlexCol, FlexRowFill } from "../styles";

const EventsDetail = () => {

  return (
  <>
    <p>Leave certifications and warnings on customer profiles and view certifications from other gyms. <span style={{fontWeight: `bold`}}>Save time</span> on thorough belay checks for veterans and <span style={{fontWeight: `bold`}}>focus</span> your supervision on the climbers who have been flagged.</p>
    </>
  );
};

export default EventsDetail;
