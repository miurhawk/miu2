import * as React from "react";
import {DetailRow, FlexCol, HalfImageText} from "../styles";

const CoursesDetail = () => {

  return (
    <>
      <p>
          From friendly competitions to ratings and reviews of the routes they attempt, get your climbers <span style={{fontWeight: `bold`}}>more involved</span>. Plus, our TapTags join the physical world with the virtual.
          Climbers and setters can simply <span style={{fontWeight: `bold`}}>tap</span> their phone or <span style={{fontWeight: `bold`}}>scan</span> a QR code to reach the route page, whether or not our app is installed.
      </p>
      </>
  );
};

export default CoursesDetail;
